<%= form_for group, url: add_groups_path(id: group.id) do |f| %>
  <% if group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "User ID:" %><br>
    <%= f.text_field :id , value: ""%>
  </div>
  <div class="actions">
    <%= f.submit "Add User" %>
  </div>
<% end %>
