<%= form_for(comprise) do |f| %>
  <% if comprise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comprise.errors.count, "error") %> prohibited this comprise from being saved:</h2>

      <ul>
      <% comprise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Group ID:" %><br>
    <%= f.text_field :group_id %>
  </div>


  <div class="field">
    <%= f.label "User ID:" %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit "Add user to group" %>
  </div>
<% end %>
