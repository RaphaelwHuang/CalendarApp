<div class="container">
<h1>Editing Group <%= @group.name %></h1>

<%= bootstrap_form_for(@group) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 
    <%= f.text_field :name, label: "Group Name*" %>
    <%= f.text_area :bio, label: "Group Description" %>
    <small><em>* Fields marked with * cannot be left blank</em></small>
    <div id ="submit">
    <%= f.submit label:"Update", class: "btn btn-success btn-lg" %>
    </div>
<% end %>
<br>

<div id="submit"><%= link_to "Delete Group", group_path(@group), class: "center btn btn-danger", data: { confirm: "Are you sure you want to delete this group? This action cannot be undone." }, method: :delete %></div>


<%= link_to 'Back', :back, class: "button" %>
</div>

